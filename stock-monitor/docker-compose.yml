version: '3.8'

services:
  stock-monitor:
    build: .
    container_name: stock-monitor
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    # 用于调试
    # command: python main.py --run-once
